name: Review Bot Hello

on:
  issue_comment:
    types: [created]

permissions:
  issues: write
  pull-requests: read

jobs:
  call-shared:
    uses: ckdwns9121/shared-actions/.github/workflows/main.yml@v1.0.2
    with:
      mention: "@review-bot"
      reply: "안녕하세요 리뷰봇입니다. 🤖 (호출 확인 완료!)"
